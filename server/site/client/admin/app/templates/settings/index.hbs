<h1 class="page-header">Configurações do sistema</h1>

{{settings-menu}}

<form {{action "save" model.settings on="submit"}}>
  <div class="row">
    <div class="col-md-8">
      <div class="panel panel-default">
        <div class="panel-heading">Dados do site:</div>
        <div class="panel-body">
          <fieldset>
            <div class="form-group">
              <label>Título do site*:</label>
              {{input value=model.settings.siteName class="form-control" placeholder="Digite o título do sistema aqui..." required="required"}}
              <p class="help-block">O "nome do site" é usado nos títulos do site e como nome do sistema.</p>
            </div>
            <div class="form-group">
              <label>Descrição:</label>
              {{textarea value=model.settings.siteDescription class="form-control" rows="3"}}
              <p class="help-block">Descrição usada nas páginas e nos compartilhamentos nas redes sociais.<br>
              Para uma melhor apresentação evite passar de 200 palavras.</p>
            </div>

            <div class="form-group">
              <label>Texto do rodapé:</label>
              {{textarea value=model.settings.siteFooter class="form-control" rows="3"}}
              <p class="help-block">O texto que é exibido na parte de baixo do site, no rodapé.</p>
            </div>

            <div class="form-group">
              <label>Assuntos (keywords):</label>
              {{input value=model.settings.metatagKeywords name="metatagKeywords" class="form-control" placeholder="Ex: tecnologia,site,comida"}}
              <p class="help-block">Palavras separadas por <strong>,</strong>.
                <br>Os assuntos são opcionais e são usados por alguns motores de busca para indexação do site.
              </p>
            </div>

            <div class="form-group">
              <label>Email do sistema*:</label>
              {{input value=model.settings.emailContact class="form-control" placeholder="Ex: nao-responda <contato@linkysystems.com>" required="required"}}
              <p class="help-block">Endereço de email do sistema que é usado para recebimento das mensagens de contato e avisos aos administradores.</p>
            </div>
          </fieldset>
        </div>
      </div>

      <div>
        <button class="btn btn-primary">
          <i class="fa fa-save"></i>
          Salvar
        </button>
      </div>
    </div>

    <div class="col-md-4 system-settings-sidebar">
      <div class="panel panel-default">
        <div class="panel-heading">Logo e ícone:</div>
        <div class="panel-body">
          <fieldset>
            <div class="form-group">
              <label>Logo do site:</label>
              {{image-uploader value=model.logo}}
              <p class="help-block">Imagem com 35px de altura (height) e no máximo 200px de largura (width).</p>
            </div>
            <div class="form-group">
              <label>Ícone do site:</label>
              {{image-uploader value=model.icon}}
              <p class="help-block">Imagem quadrada com 32x23px ou 48x48px ou 64x64px ou 128x128px.</p>
            </div>
            <div class="form-group">
              <label>Imagem para redes sociais:</label>
              {{image-uploader value=model.ogImage}}
              <p class="help-block">Imagem com 1200x630px.
                <br>Essa imagem é exibida ao compartilhar páginas sem imagens de destaque nas redes sociais.
              </p>
            </div>
          </fieldset>

          <div>
            <button class="btn btn-primary">
              <i class="fa fa-save"></i>
              Salvar
            </button>
          </div>

        </div>
      </div>
    </div>
  </div>
</form>
