<h1 class="page-header">{{t 'vocabulary.find'}}</h1>

<div class="actions">
  {{#link-to 'vocabulary.create' class="btn btn-default"}}
    <i class="glyphicon glyphicon-plus"></i> {{t 'vocabulary.create'}}
  {{/link-to}}
</div>
<br>

{{conteudos-table data=model.records columns=model.columns pageSize=25 useFilteringByColumns=false deleteRecord='deleteRecord'}}